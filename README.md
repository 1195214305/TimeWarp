# TimeWarp - 边缘时光机

<div align="center">

![TimeWarp Logo](frontend/public/favicon.svg)

**探索你脚下这片土地的千年历史**

本项目由[阿里云ESA](https://www.aliyun.com/product/esa)提供加速、计算和保护

![Aliyun ESA](https://img.alicdn.com/imgextra/i3/O1CN01H1UU3i1Cti9lYtFrs_!!6000000000139-2-tps-7534-844.png)

[![Powered by Aliyun ESA](https://img.shields.io/badge/Powered%20by-Aliyun%20ESA%20Pages-FF6A00?style=for-the-badge&logo=alibabacloud)](https://www.aliyun.com/product/esa)
[![AI Powered](https://img.shields.io/badge/AI-通义千问-6366F1?style=for-the-badge)](https://tongyi.aliyun.com/)
[![React](https://img.shields.io/badge/React-18.2-61DAFB?style=for-the-badge&logo=react)](https://react.dev/)

[在线体验](#在线体验) | [功能特性](#功能特性) | [技术架构](#技术架构) | [边缘计算应用](#边缘计算应用-how-we-use-edge)

#阿里云ESA Pages #阿里云云工开物

</div>

---

## 项目简介

**TimeWarp** 是一款基于阿里云 ESA Pages 边缘计算平台构建的沉浸式历史体验应用。它利用 Geo-IP 技术自动识别用户地理位置，结合通义千问大模型实时生成该地点在不同历史时期的故事，让用户仿佛穿越时空，感受历史的温度。

### 核心亮点

- 🌍 **Geo-IP 智能定位** - 基于 ESA 边缘节点自动识别用户位置，无需手动输入
- ⏳ **多时代穿越** - 从远古到当代，选择不同时代探索同一地点的历史变迁
- 📖 **AI 沉浸式叙事** - 通义千问大模型生成第二人称历史故事，身临其境
- ⚡ **流式响应** - 边缘函数流式输出，实时感受 AI 创作过程
- 🎨 **复古设计** - 精心设计的复古纸张质感，营造历史氛围

---

## 功能演示

### 首页 - 开始历史之旅
![首页](screenshots/homepage.png)
*复古纸张质感设计，输入地点或使用当前位置开始探索*

### 探索页 - 穿越时空
![探索页](screenshots/explore.png)
*选择不同时代，AI 实时生成沉浸式历史故事*

### 时间线 - 历史事件
![时间线](screenshots/timeline.png)
*重要历史事件时间线，了解这片土地的沧桑变迁*

---

## 功能特性

### 核心功能

| 功能 | 描述 |
|------|------|
| 🗺️ **智能定位** | 基于 Geo-IP 自动识别用户位置，推荐相关历史内容 |
| ⏰ **时代选择** | 5 个历史时代可选：远古、帝国、近代、当代、近年 |
| 📜 **AI 故事生成** | 通义千问生成沉浸式第二人称历史叙述 |
| 💾 **时光胶囊** | 保存喜欢的历史故事，随时回顾 |
| 🔗 **分享功能** | 一键分享历史故事链接 |
| 📱 **响应式设计** | 完美适配桌面端和移动端 |

### 历史时代

| 时代 | 时间范围 | 特点 |
|------|----------|------|
| 🏛️ 远古时代 | 公元前3000年-公元前221年 | 华夏文明起源，夏商周三代 |
| 👑 帝国时代 | 公元前221年-1912年 | 秦汉唐宋元明清，两千年帝制 |
| 🏭 近代 | 1912年-1949年 | 辛亥革命到新中国成立 |
| 🌆 当代 | 1949年-2000年 | 社会主义建设时期 |
| 🌐 近年 | 2000年-至今 | 现代化快速发展时期 |

---

## 技术架构

### 整体架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                          用户浏览器                                │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  React 18 + Vite + TailwindCSS + Framer Motion            │  │
│  │  ├── 复古纸张质感 UI                                        │  │
│  │  ├── Zustand 状态管理 (时光胶囊持久化)                      │  │
│  │  └── 流式内容渲染                                          │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────────┐
│                      阿里云 ESA Pages                             │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                   全球边缘节点网络                           │  │
│  │  ┌──────────────────────────────────────────────────────┐  │  │
│  │  │              Geo-IP 地理位置识别                       │  │  │
│  │  │  - x-geo-country: 国家代码                            │  │  │
│  │  │  - x-geo-city: 城市名称                               │  │  │
│  │  │  - x-geo-latitude/longitude: 经纬度                   │  │  │
│  │  └──────────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                    边缘函数 (Edge Functions)                │  │
│  │  ├── /api/edge/info       - Geo-IP 信息 & 历史推荐         │  │
│  │  ├── /api/history/story   - AI 历史故事生成 (流式)         │  │
│  │  ├── /api/history/events  - 历史事件查询                   │  │
│  │  └── /api/capsule/save    - 时光胶囊存储                   │  │
│  └────────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                    边缘缓存 (Edge Cache)                    │  │
│  │  ├── 热门地点历史数据缓存                                   │  │
│  │  └── 静态资源全球加速                                       │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────────┐
│                      阿里云 AI 服务                               │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │   通义千问 (qwen-turbo)                                     │  │
│  │   - 沉浸式历史故事生成                                      │  │
│  │   - 第二人称叙事风格                                        │  │
│  │   - 流式响应支持                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 边缘计算应用 (How We Use Edge)

TimeWarp 深度利用阿里云 ESA 的边缘计算能力，以下是核心应用场景：

### 1. Geo-IP 地理位置感知（核心特性）

```javascript
// functions/edge/info.js
export async function onRequest(context) {
  const { request } = context;

  // 获取 ESA 提供的地理位置信息
  const geoInfo = {
    country: request.headers.get('x-geo-country'),
    city: request.headers.get('x-geo-city'),
    latitude: request.headers.get('x-geo-latitude'),
    longitude: request.headers.get('x-geo-longitude'),
    timezone: request.headers.get('x-geo-timezone'),
  };

  // 根据城市推荐相关历史内容
  const recommendations = getHistoricalRecommendations(geoInfo.city);

  return new Response(JSON.stringify({
    geo: geoInfo,
    recommendations: recommendations,
  }));
}
```

**应用场景**：
- 自动识别用户所在城市，无需手动输入
- 根据地理位置推荐相关历史内容
- 显示"你脚下这片土地"的历史

### 2. 流式响应 AI 生成

```javascript
// functions/history/story.js
export async function onRequest(context) {
  const { location, era } = await request.json();

  // 调用通义千问 API（流式模式）
  const response = await fetch(QWEN_API_URL, {
    body: JSON.stringify({
      stream: true,
      messages: [
        { role: 'system', content: '你是一位历史学家...' },
        { role: 'user', content: `讲述${location}在${era}的历史` },
      ],
    }),
  });

  // 边缘侧处理流式数据
  const { readable, writable } = new TransformStream();
  // ... 流式处理逻辑

  return new Response(readable, {
    headers: { 'Transfer-Encoding': 'chunked' },
  });
}
```

**应用场景**：
- AI 历史故事实时生成
- 用户可以看到文字逐渐出现，增强沉浸感
- 边缘侧处理，减少延迟

### 3. 边缘缓存策略

```javascript
// 热门历史地点数据缓存
// - 北京、上海、西安等热门城市的历史数据
// - 缓存在边缘节点，全球用户快速访问

// 静态资源缓存
// - 复古纸张纹理背景
// - 字体文件
// - 图标资源
```

### 4. 基于位置的内容推荐

```javascript
// 根据用户位置推荐历史内容
function getHistoricalRecommendations(city) {
  const recommendations = {
    '北京': [
      { era: 'imperial', title: '紫禁城的故事' },
      { era: 'modern', title: '五四运动' },
    ],
    '上海': [
      { era: 'modern', title: '十里洋场' },
      { era: 'contemporary', title: '浦东开发' },
    ],
    // ...
  };
  return recommendations[city] || defaultRecommendations;
}
```

---

## 项目结构

```
10_TimeWarp_边缘时光机/
├── frontend/                      # 前端代码
│   ├── src/
│   │   ├── components/           # React 组件
│   │   ├── pages/                # 页面
│   │   │   ├── HomePage.jsx      # 首页
│   │   │   ├── ExplorePage.jsx   # 探索页
│   │   │   └── StoryPage.jsx     # 故事详情页
│   │   ├── store/                # 状态管理
│   │   │   └── index.js          # Zustand Store
│   │   ├── utils/                # 工具函数
│   │   │   └── api.js            # API 调用
│   │   ├── App.jsx               # 应用入口
│   │   ├── main.jsx              # 渲染入口
│   │   └── index.css             # 全局样式（复古主题）
│   ├── public/                   # 静态资源
│   │   └── favicon.svg           # 时光机图标
│   ├── index.html                # HTML 模板
│   ├── package.json              # 依赖配置
│   ├── vite.config.js            # Vite 配置
│   ├── tailwind.config.js        # Tailwind 配置
│   └── postcss.config.js         # PostCSS 配置
├── functions/                     # 边缘函数
│   ├── edge/
│   │   └── info.js               # Geo-IP 信息
│   └── history/
│       └── story.js              # AI 历史故事生成
├── screenshots/                   # 截图
└── README.md                      # 项目文档
```

---

## 快速开始

### 本地开发

```bash
# 1. 进入前端目录
cd frontend

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 4. 访问 http://localhost:5191
```

### 生产部署

1. 在阿里云 ESA 控制台创建 Pages 项目
2. 关联 GitHub 仓库
3. 配置构建命令：
   - 构建命令：`npm install && npm run build`
   - 根目录：`/frontend`
   - 输出目录：`dist`
4. 部署完成后，ESA 会自动在全球边缘节点同步

---

## 技术栈

| 类别 | 技术 |
|------|------|
| **前端框架** | React 18 + Vite |
| **样式方案** | TailwindCSS 3 (复古主题) |
| **动画库** | Framer Motion |
| **状态管理** | Zustand (持久化) |
| **路由** | React Router 6 |
| **日期处理** | date-fns |
| **部署平台** | 阿里云 ESA Pages |
| **边缘计算** | ESA Edge Functions |
| **AI 服务** | 通义千问 (qwen-turbo) |

---

## 设计理念

### 复古时光主题

本项目采用复古纸张质感设计，营造历史氛围：

1. **色彩系统**：
   - Sepia 系列：复古棕色，营造老照片质感
   - Temporal 系列：深邃蓝紫，代表时空穿越
   - Antique 系列：暖金色，象征历史的厚重

2. **纹理背景**：
   - 复古纸张纹理 SVG 背景
   - 时光粒子动画效果

3. **字体选择**：
   - 标题：Noto Serif SC（宋体）
   - 正文：Inter（无衬线）

---

## 应用场景

1. **历史教育** - 让学生身临其境感受历史
2. **旅游导览** - 到达景点时了解当地历史
3. **文化传承** - 探索家乡的历史变迁
4. **休闲阅读** - 沉浸式历史故事体验

---

## 许可证

MIT License

---

<div align="center">

**TimeWarp** - 探索历史的温度

Made with ❤️ for 阿里云 ESA Pages 边缘开发大赛

</div>
